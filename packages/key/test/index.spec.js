/**
 * @jest-environment jsdom
 */
import React from "react";
import useKey from "..";

describe("useKey", () => {
  it("should be defined", () => {
    expect(useKey).toBeDefined();
  });
});

// figure out tests
