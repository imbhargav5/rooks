/**
 * @jest-environment jsdom
 */
import React from "react";
import useKeys from "..";

describe("useKeys", () => {
  it("should be defined", () => {
    expect(useKeys).toBeDefined();
  });
});

// figure out tests
