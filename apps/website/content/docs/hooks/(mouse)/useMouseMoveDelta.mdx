---
id: useMouseMoveDelta
title: useMouseMoveDelta
sidebar_label: useMouseMoveDelta
description: "Tracks the delta (change) in mouse position between consecutive mousemove events."
---

## About

Tracks delta of mouse move

## Examples

```tsx
import {useMouseMoveDelta} from "rooks"
export default function App() {
  useMouseMoveDelta();
  return null
}
```

<LiveExample lang="tsx" code={`import {useMouseMoveDelta} from "rooks"
export default function App() {
  useMouseMoveDelta();
  return null
}
`} />
