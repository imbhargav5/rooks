---
id: useWindowEventListener
title: useWindowEventListener
sidebar_label: useWindowEventListener
description: "Attaches and automatically cleans up an event listener on the window."
---

## About

Adds an event listener to window

[//]: # "Main"

## Examples

### Basic usage

<LiveExample code={`import { useState } from "react";
import { useWindowEventListener } from "rooks";
import "./styles.css";

export default function App() {
  const [value, setValue] = useState(0);
  useWindowEventListener("click", function() {
    setValue(value + 1);
  });
  return (
    <div
      className="App"
      style={{
        padding: "20px",
      }}
    >
      <h2>Click anywhere</h2>
      <p> Value is {value}</p>
    </div>
  );
}
`} />

---
